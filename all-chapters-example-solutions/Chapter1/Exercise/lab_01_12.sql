SELECT
    employee_id
     || ','
     || first_name
     || ','
     || last_name
     || ','
     || phone_number
     || ','
     || job_id
     || ','
     || manager_id
     || ','
     || hire_date
     || ','
     || salary
     || ',,'
     || department_id
     The_Output
FROM
    employees;